{
  "name": "hackwestern-chatbot-nonsense",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npx nodemon",
    "start": "npm run build && node ./dist/index.js",
    "build": "rimraf ./dist && tsc"
  },
  "author": "Mike Elsmore <mike.elsmore@infobip.com>",
  "license": "MIT",
  "dependencies": {
    "@infobip-api/sdk": "^0.3.1",
    "dotenv": "^16.3.1",
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.2",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5"
  }
}
